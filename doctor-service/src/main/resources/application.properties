spring.application.name=doctor-service

# Server Configuration
server.port=8082

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/synapscare_doctors
spring.datasource.username=postgres
spring.datasource.password=Luqa
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# RabbitMQ Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=admin
spring.rabbitmq.password=admin123

# JWT Configuration (same secret as auth-service for validation)
app.jwt.secret=ejedVZ4KIRRaAy8kWjs4QSadL823O1WKUDanGentLY3
app.jwt.expiration-ms=3600000
app.jwt.refresh-expiration-ms=604800000

# Auth Service Configuration
app.auth-service.url=http://localhost:8081

# Docker Compose (disabled for local development)
spring.docker.compose.enabled=false
